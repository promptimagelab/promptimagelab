{
  "name": "promptimagelab",
  "version": "2.0.0",
  "description": "PromptImageLab — AI Image Prompt Library on Cloudflare Pages",
  "scripts": {
    "dev": "npx wrangler pages dev public --compatibility-date=2024-09-23",
    "deploy": "npx wrangler pages deploy public --project-name=promptimagelab"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
